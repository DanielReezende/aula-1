import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    paddingVertical: 4,
    paddingHorizontal: 16,

    backgroundColor: '#fff',
    borderRadius: 4,
  }
});


